{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-success btn-sm float-right">
	<i class="fa fa-copy"></i>
	|
	{{ button_label|default('Ajouter') }}
</button>
{{ form_end(form) }}
