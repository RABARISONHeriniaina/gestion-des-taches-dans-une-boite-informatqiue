{% extends 'layouts/base.html.twig' %}
{% block body %}
	<div class="content-wrapper">
		<div class="content-header">
			<div class="container">
				<div class="row mb-2">
					<h1>Modification d'un poste</h1>
				</div>
			</div>
		</div>
		<div class="content">
			<div class="container">
				<div class="row">

					{{ include('job/_form.html.twig', {'button_label': 'Update'}) }}

					<a href="{{ path('job_index') }}">back to list</a>

					{{ include('job/_delete_form.html.twig') }}
				</div>
			</div>
		</div>
	</div>


{% endblock %}
