<form style="display: none" id="form_delete{{ job.id }}" method="post" action="{{ path('job_delete', {'id': job.id}) }}">
	<input type="hidden" name="_token_delete" value="{{ csrf_token('delete' ~ job.id) }}">
	<button class="btn">Delete</button>
</form>
