{% extends 'layouts/base.html.twig' %}

{% block body %}
	<div class="content-wrapper">

		<div class="content">
			<div class="container">
				<div class="row">
					<div class="col-lg-12 mt-3 h2 text-primary">Modification d'un employee</div>
					<div class="col-lg-12 mt-3">
						{{ include('employee/_form.html.twig', {'button_label': 'Update'}) }}

						<a href="{{ path('employee_index') }}">back to list</a>

						{{ include('employee/_delete_form.html.twig') }}</div>
				</div>
			</div>
		</div>
	</div>

{% endblock %}
